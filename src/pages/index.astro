---
import Base from "@layouts/Base.astro";
import { getArticlesWithOk } from "@lib/collections/articles";

const okArticles = await getArticlesWithOk();
---

<Base title="PCF">
  <main>
    <ul class="menu">
      {
        okArticles.map((article) => (
          <li>
            <a href={`/${article.data}`}>{article.data.title}</a>
          </li>
        ))
      }
    </ul>
  </main>
</Base>
